# Phase 31.1 local development defaults
# Never commit real secrets. Use separate values for dev, staging and production.
NODE_ENV=development
NEXT_PUBLIC_APP_URL=http://localhost:3000
BACKEND_URL=http://127.0.0.1:8000
WEB_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Reserved for later phases. Do not add live values until the relevant phase.
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
AI_GATEWAY_PROVIDER=
